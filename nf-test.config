plugins {
    id 'nft-utils@0.0.3'
}

testsDir "${projectDir}/tests"
workDir  "${params.outdir ?: 'tests/results'}/work"

profiles {
    test {
        includeConfig "${projectDir}/conf/test.config"
    }
}
