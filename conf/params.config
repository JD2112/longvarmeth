params {
    // Input/Output options
    input               = "samplesheet.csv"
    outdir              = "results"
    
    // Workflow behavior defaults
    species             = "eukaryote" // human, eukaryote, prokaryote, virus
    run_type            = "reference_based" // reference_based, de_novo_assembly
    
    // Dorado GPU parameters (executed on host, not containerized)
    dorado_bin          = "/data/Jyotirmoy/tools/dorado-0.9.6-linux-x64/bin/dorado"
    gpu_devices         = "cuda:all"
    threads             = 32
    
    // Quality thresholds
    min_barcode_size_mb = 100
    filt_min_len        = 1000
    filt_keep_pct       = 90
    
    // References (default settings)
    ref_nuc             = ""
    ref_mt_ap           = ""
    ref_mt_au           = ""
    
    // Module default options
    enable_phasing      = false
    medaka_model        = "r1041_e82_400bps_hac_v4.0.0"
    busco_lineage       = "eukaryota"
    methylartist_locus  = ""

    // Resource limit defaults
    max_memory          = '500.GB'
    max_cpus            = 128
    max_time            = '240.h'

    // Annotation options
    enable_snpeff       = false
    snpeff_db           = "GRCh38.105"
    snpeff_cache        = ""
    
    enable_vep          = false
    vep_species         = "homo_sapiens"
    vep_assembly        = "GRCh38"
    vep_version         = "110"
    vep_cache           = ""
}
